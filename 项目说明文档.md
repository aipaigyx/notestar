# 笔记星图（NoteStar）— 项目全方位说明文档

> 版本：1.0.0 | 更新日期：2026-08-11 | 技术栈：Electron 30 + Vue 3 + TypeScript + Vite

---

## 目录

1. [项目概述](#1-项目概述)
2. [核心工作流](#2-核心工作流)
3. [技术架构](#3-技术架构)
4. [项目结构](#4-项目结构)
5. [功能清单](#5-功能清单)
6. [前后端接口文档](#6-前后端接口文档)
7. [数据模型](#7-数据模型)
8. [AI 集成方案](#8-ai-集成方案)
9. [数据存储与持久化](#9-数据存储与持久化)
10. [安全设计](#10-安全设计)
11. [构建与运行](#11-构建与运行)
12. [打包发布](#12-打包发布)
13. [已知限制与未来计划](#13-已知限制与未来计划)

---

## 1. 项目概述

### 1.1 产品定位

**笔记星图**是一款基于 AI 的桌面端学习笔记管理工具，面向需要将课堂录音转写为结构化笔记的学生群体。

### 1.2 核心价值

传统流程：录音 → 手动听写 → 手动整理笔记 → 手动复习

笔记星图：录音 → 第三方工具转文字 → **导入 .txt 文件 → AI 自动整理成结构化笔记 → 基于笔记进行 AI 问答**

程序从空状态启动，不包含任何预设数据。所有数据由用户创建，所有 AI 功能调用真实的 DeepSeek API。

### 1.3 设计理念

- **零模拟数据** — 所有数据从空状态开始，由用户操作产生
- **AI 驱动** — 接入 DeepSeek 大语言模型，非本地模拟
- **本地优先** — 数据存储在本地 JSON 文件，不上传服务器
- **桌面体验** — Electron 打包为原生 Windows EXE 程序

---

## 2. 核心工作流

### 2.1 笔记生成流程

```
用户录制课堂音频
        │
        ▼
使用第三方工具（如通义听悟、飞书妙记）将音频转为文字
        │
        ▼
导出为 .txt 文件
        │
        ▼
┌─────────────────────────────────────────┐
│  笔记星图                                │
│                                         │
│  1. 打开「笔记整理」页面                  │
│  2. 点击「导入」→ 选择 .txt 文件          │
│     或直接粘贴转写文本                   │
│  3. 点击「AI 整理」                      │
│  4. DeepSeek API 流式生成结构化笔记       │
│     （打字机效果实时显示）                │
│  5. 自动创建笔记，归入课程                │
│  6. 记录 10 分钟学习时间                  │
└─────────────────────────────────────────┘
```

### 2.2 AI 问答流程

```
用户打开「AI 助手」
        │
        ▼
系统自动构建笔记上下文（最多 4000 字符）
        │
        ▼
用户输入问题
        │
        ▼
DeepSeek API 基于笔记上下文 + 对话历史
        │
        ▼
流式返回回答（打字机效果）
        │
        ▼
自动保存对话记录，记录 2 分钟学习时间
```

### 2.3 知识图谱生成

```
用户笔记数据（课程 + 笔记标题 + 标签）
        │
        ▼
前端 computed 自动计算节点和连线
        │
        ▼
中心节点 ← 课程节点 ← 笔记节点 ← 标签节点
        │
        ▼
SVG 渲染力导向布局图谱
        │
        ▼
点击节点查看详情 / AI 分析知识点关系
```

---

## 3. 技术架构

### 3.1 整体架构

```
┌──────────────────────────────────────────────────────┐
│                  Electron 主进程                      │
│                                                      │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │  IPC Handlers │  │ DeepSeek API  │  │ 文件系统    │  │
│  │  (23个端点)   │  │  (https模块)   │  │ (JSON存储)  │  │
│  └──────┬──────┘  └──────┬───────┘  └─────┬──────┘  │
│         │                │                 │          │
│         └────────────────┴─────────────────┘          │
│                          │                            │
│                    preload.js (安全桥接)               │
│                          │                            │
├──────────────────────────┼────────────────────────────┤
│                    渲染进程 (Renderer)                  │
│                          │                            │
│  ┌──────────┐  ┌────────┴────────┐  ┌─────────────┐  │
│  │ Vue 3 App │  │   store.ts      │  │  vue-router  │  │
│  │ (5个页面)  │←→│ (响应式状态管理)  │←→│  (Hash路由)  │  │
│  └──────────┘  └─────────────────┘  └─────────────┘  │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 3.2 技术选型

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| 桌面框架 | Electron | ^30.0.0 | 跨平台桌面应用，原生系统访问 |
| 前端框架 | Vue 3 | ^3.4.21 | Composition API，响应式数据驱动 |
| 类型系统 | TypeScript | ^5.4.0 | 类型安全，IPC 接口类型声明 |
| 构建工具 | Vite | ^5.2.0 | 快速热更新，原生 ESM |
| 路由 | Vue Router | ^4.3.0 | Hash 模式路由，适配 Electron |
| Markdown | marked | ^18.0.9 | 轻量 Markdown → HTML 渲染 |
| AI 模型 | DeepSeek | deepseek-chat | 中文理解能力强，性价比高 |
| 打包工具 | electron-builder | ^24.13.3 | Windows NSIS 安装包 |

### 3.3 进程模型

```
主进程 (main.cjs)
  ├── 窗口管理 (BrowserWindow)
  ├── IPC 通信 (ipcMain.handle)
  ├── 文件 I/O (fs)
  ├── HTTP 请求 (https → DeepSeek API)
  └── 系统对话框 (dialog)

预加载脚本 (preload.js)
  ├── contextBridge.exposeInMainWorld('noteAPI', ...)
  └── ipcRenderer.invoke / ipcRenderer.on

渲染进程 (Vue 3)
  ├── store.ts — 全局状态管理 + IPC 调用
  ├── 5 个页面组件
  └── 1 个侧边栏组件
```

---

## 4. 项目结构

```
app/
├── electron/                    # Electron 主进程
│   ├── main.cjs                 # 主进程入口（IPC + AI + 文件系统）
│   ├── preload.js               # 预加载脚本（安全 IPC 桥接）
│   └── test.js                  # 开发调试文件
│
├── src/                         # 前端源码
│   ├── main.ts                  # 应用入口 + 路由配置
│   ├── App.vue                  # 根组件（布局 + 侧边栏）
│   ├── store.ts                 # 全局状态管理（427行）
│   ├── types.ts                 # TypeScript 类型定义
│   ├── style.css                # 全局样式 + CSS 变量
│   ├── env.d.ts                 # 环境类型声明
│   │
│   ├── components/
│   │   └── Sidebar.vue          # 侧边栏导航组件
│   │
│   └── views/                   # 页面组件
│       ├── Dashboard.vue        # 仪表盘（统计 + 搜索 + 快捷入口）
│       ├── NoteOrganize.vue     # 笔记整理（导入 + 编辑器 + AI整理）
│       ├── KnowledgeGraph.vue   # 知识图谱（动态生成 + SVG渲染）
│       ├── AIAssistant.vue      # AI助手（流式对话 + 历史管理）
│       └── Settings.vue         # 设置（API Key + 数据管理）
│
├── data/                        # 数据存储目录（运行时生成）
│   ├── notes.json               # 笔记数据
│   ├── courses.json             # 课程数据
│   ├── chatSessions.json        # 对话记录
│   ├── stats.json               # 统计数据
│   └── settings.json            # 用户设置（API Key）
│
├── dist/                        # 构建输出（vite build 生成）
├── userdata/                    # Electron 用户数据（缓存等）
│
├── package.json                 # 项目配置 + 依赖
├── vite.config.ts               # Vite 构建配置
├── tsconfig.json                # TypeScript 配置
├── tsconfig.node.json           # Node.js TypeScript 配置
└── index.html                   # HTML 入口
```

---

## 5. 功能清单

### 5.1 功能矩阵

| 模块 | 功能 | 实现状态 | 技术实现 |
|------|------|----------|----------|
| **笔记整理** | 文件导入（.txt/.md） | ✅ | `dialog.showOpenDialog` + `fs.readFileSync` |
| | 粘贴文本导入 | ✅ | 前端 textarea |
| | AI 流式整理笔记 | ✅ | DeepSeek streaming + IPC chunk 推送 |
| | Markdown 编辑 | ✅ | textarea 编辑器 |
| | Markdown 预览 | ✅ | `marked.parse()` 渲染 |
| | 笔记 CRUD | ✅ | JSON 文件持久化 |
| | 笔记搜索 | ✅ | 实时过滤标题 + 内容 |
| | 课程筛选 | ✅ | `route.query.course` 传递 |
| | AI 总结摘要 | ✅ | DeepSeek API + 自动保存 |
| | 大纲自动生成 | ✅ | 解析 Markdown 标题层级 |
| | 标签管理 | ✅ | 笔记 tags 字段 |
| **AI 助手** | 流式对话 | ✅ | 打字机效果 + 光标闪烁 |
| | 多轮对话 | ✅ | 历史消息上下文（最多6轮） |
| | 笔记上下文 | ✅ | 自动构建（最多4000字符） |
| | 对话历史 | ✅ | 会话列表 + 切换 + 删除 |
| | Markdown 渲染 | ✅ | AI 回复渲染为 HTML |
| | 建议问题 | ✅ | 3个预设建议卡片 |
| | 重新生成 | ✅ | 重新调用 API |
| | 错误处理 | ✅ | API Key 缺失 + 网络错误提示 |
| **知识图谱** | 动态节点生成 | ✅ | 基于笔记/课程/标签 computed |
| | SVG 渲染 | ✅ | 力导向布局 + 连线 |
| | 节点详情面板 | ✅ | 统计 + 关联笔记列表 |
| | AI 知识分析 | ✅ | DeepSeek 分析知识点关系 |
| | 导出图片 | ✅ | SVG → PNG 下载 |
| | 空状态引导 | ✅ | 无笔记时显示引导 |
| **仪表盘** | 统计卡片 | ✅ | 笔记数/知识点/学习时长/课程数 |
| | 连续打卡 | ✅ | 火焰徽章 + streakDays |
| | 本周柱状图 | ✅ | 7天学习时间，今天高亮 |
| | 搜索传递 | ✅ | `route.query.q` → 笔记页 |
| | 快速提问 | ✅ | `route.query.q` → AI助手页 |
| | AI 学习面板 | ✅ | 学习概览 + 快捷操作 |
| | 最近笔记 | ✅ | 按 updatedAt 排序 |
| | 最近提问 | ✅ | 最近对话记录 |
| **设置** | API Key 配置 | ✅ | 加密存储在 settings.json |
| | 模型选择 | ✅ | deepseek-chat |
| | 数据导出 | ✅ | JSON 备份文件 |
| | 数据导入 | ✅ | JSON 恢复 + 刷新 |
| | 数据清空 | ✅ | 全部/笔记/对话/统计 |
| | 使用说明 | ✅ | 4步引导 |
| **侧边栏** | 导航菜单 | ✅ | 4个页面路由 |
| | 课程列表 | ✅ | 显示 + 笔记计数 |
| | 课程删除 | ✅ | 悬停删除按钮 + 确认 |
| | 课程跳转 | ✅ | 点击 → 笔记页 + 筛选 |
| | 打卡显示 | ✅ | streakDays > 0 时显示 |
| **全局** | 学习时间记录 | ✅ | 导入+10min, 对话+2min |
| | 连续打卡计算 | ✅ | 后端日期比对 |
| | 空状态引导 | ✅ | 所有页面从空开始 |
| | 路由动画 | ✅ | fade 过渡 |

### 5.2 页面路由

| 路径 | 组件 | 页面名称 | 说明 |
|------|------|----------|------|
| `/dashboard` | Dashboard.vue | 仪表盘 | 统计概览 + 快捷操作 |
| `/notes` | NoteOrganize.vue | 笔记整理 | 三栏布局：列表 + 编辑器 + 大纲 |
| `/graph` | KnowledgeGraph.vue | 知识图谱 | SVG 动态图谱 |
| `/assistant` | AIAssistant.vue | AI 助手 | 对话界面 + 历史列表 |
| `/settings` | Settings.vue | 设置 | API配置 + 数据管理 |

---

## 6. 前后端接口文档

### 6.1 IPC 端点总览

程序采用 Electron IPC（`ipcMain.handle` / `ipcRenderer.invoke`）作为前后端通信机制，共 **23 个端点**：

| # | Channel | 方向 | 请求参数 | 响应 | 说明 |
|---|---------|------|----------|------|------|
| 1 | `settings:get` | invoke | — | `Settings` | 获取用户设置 |
| 2 | `settings:save` | invoke | `Settings` | `boolean` | 保存用户设置 |
| 3 | `files:import` | invoke | — | `ImportedFile[] \| null` | 打开文件选择对话框 |
| 4 | `ai:generateNote` | invoke | `string` (rawText) | `string` (Markdown) | AI 整理转写文本 |
| 5 | `ai:generateNote:chunk` | send← | — | `string` (chunk) | 流式笔记生成 chunk |
| 6 | `ai:chat` | invoke | `(string, string, ChatMessage[]?)` | `string` | AI 对话 |
| 7 | `ai:chat:chunk` | send← | — | `string` (chunk) | 流式对话 chunk |
| 8 | `ai:summarize` | invoke | `string` (noteContent) | `string` | AI 生成摘要 |
| 9 | `notes:getAll` | invoke | — | `Note[]` | 获取所有笔记 |
| 10 | `notes:get` | invoke | `string` (id) | `Note \| null` | 获取单篇笔记 |
| 11 | `notes:save` | invoke | `Partial<Note>` | `Note` | 创建/更新笔记 |
| 12 | `notes:delete` | invoke | `string` (id) | `boolean` | 删除笔记 |
| 13 | `courses:getAll` | invoke | — | `Course[]` | 获取所有课程 |
| 14 | `courses:save` | invoke | `Partial<Course>` | `Course` | 创建/更新课程 |
| 15 | `courses:delete` | invoke | `string` (id) | `boolean` | 删除课程 |
| 16 | `chat:getSessions` | invoke | — | `ChatSession[]` | 获取所有对话 |
| 17 | `chat:saveSession` | invoke | `Partial<ChatSession>` | `ChatSession` | 创建/更新对话 |
| 18 | `chat:deleteSession` | invoke | `string` (id) | `boolean` | 删除对话 |
| 19 | `stats:get` | invoke | — | `Stats` | 获取统计数据 |
| 20 | `stats:addStudyTime` | invoke | `number` (minutes) | `Stats` | 添加学习时间 |
| 21 | `data:export` | invoke | — | `string \| null` (filePath) | 导出数据 |
| 22 | `data:import` | invoke | — | `{success, notes, courses} \| null` | 导入数据 |
| 23 | `data:clear` | invoke | `'all'\|'notes'\|'chat'\|'stats'\|'courses'` | `boolean` | 清空数据 |

### 6.2 通信链路

```
Vue 组件 → store.ts 函数 → window.noteAPI.* → preload.js → ipcRenderer.invoke → main.cjs → IPC Handler
                                                                                              │
                                                                                              ▼
                                                                                    文件系统 / DeepSeek API
                                                                                              │
                                                                                              ▼
                                                                          ipcMain.handle 返回结果 ←  ←  ←
```

### 6.3 流式响应机制

```
1. 前端调用 store.generateNoteFromText(rawText, onChunk)
2. store.ts 调用 window.noteAPI.onGenerateNoteChunk(onChunk) 注册监听
3. preload.js: removeAllListeners → ipcRenderer.on('ai:generateNote:chunk', callback)
4. store.ts 调用 window.noteAPI.generateNote(rawText) → ipcRenderer.invoke('ai:generateNote')
5. main.cjs: callDeepSeek(apiKey, messages, (chunk) => {
     event.sender.send('ai:generateNote:chunk', chunk)  // 逐 chunk 推送
   })
6. 前端 onChunk 回调实时更新 streamingText → 打字机效果
7. invoke resolve 返回完整文本 → 前端更新最终消息
```

**关键设计：** `removeAllListeners` 确保每次调用只注册一个监听器，避免重复触发。

### 6.4 错误处理链路

```
DeepSeek API 返回非200  →  reject(Error(`API 返回 ${statusCode}`))
                              │
                              ▼
main.cjs: ipcMain.handle 自动将 Error 传递给前端
                              │
                              ▼
preload.js: ipcRenderer.invoke reject(error)
                              │
                              ▼
store.ts: 函数 throw error（不 catch，向上传递）
                              │
                              ▼
Vue 组件: try { ... } catch (e: any) {
            errorMsg.value = e.message    // 显示在 UI
          }
```

| 错误场景 | 处理方式 |
|----------|----------|
| API Key 未配置 | `throw new Error('未配置 API Key，请先在设置中填写')` |
| API 返回错误 | `reject(new Error('API 返回 ${statusCode}: ${body}'))` |
| 请求超时 | 60秒超时 → `reject(new Error('请求超时'))` |
| 网络错误 | `req.on('error', reject)` |
| 数据导入格式错误 | `throw new Error('导入失败：文件格式不正确')` |

---

## 7. 数据模型

### 7.1 TypeScript 接口定义

```typescript
// 笔记
interface Note {
  id: string              // 'n' + Date.now()
  title: string           // 笔记标题
  courseId: string        // 所属课程ID
  tags: string[]          // 标签列表
  content: string         // Markdown 内容
  createdAt: string       // ISO 8601
  updatedAt: string       // ISO 8601
  paragraphs: number      // 段落数（自动计算）
}

// 课程
interface Course {
  id: string              // 'c' + Date.now()
  name: string            // 课程名称
  noteCount: number       // 笔记数量（自动计算）
  color: string           // 十六进制颜色
  active?: boolean        // 激活状态
}

// 对话消息
interface ChatMessage {
  role: 'user' | 'assistant'
  content: string         // 消息内容
  time: string            // 'HH:MM' 格式
}

// 对话会话
interface ChatSession {
  id: string              // 'chat' + Date.now()
  title: string           // 会话标题
  messages: ChatMessage[] // 消息列表
  createdAt: string       // ISO 8601
  noteRefs?: string[]     // 关联笔记ID
}

// 统计数据
interface Stats {
  totalStudyMinutes: number    // 总学习时长（分钟）
  streakDays: number           // 连续打卡天数
  lastStudyDate: string        // 最后学习日期 'YYYY-MM-DD'
  weeklyMinutes: number[]      // 本周学习时间 [周一~周日]
  mastery: { courseId: string; pct: number }[]  // 掌握度
}

// 用户设置
interface Settings {
  apiKey: string          // DeepSeek API Key
  model: string           // 模型名称 'deepseek-chat'
}

// 导入的文件
interface ImportedFile {
  name: string            // 文件名
  content: string         // 文件内容
}
```

### 7.2 数据关系图

```
Course (1) ──────< (N) Note
  │                    │
  │                    │
  │                    ▼
  │              Tags (多对多)
  │
  ▼
Stats.mastery
  └─ { courseId, pct }

ChatSession (1) ──────< (N) ChatMessage
      │
      └─ noteRefs[] ──→ Note.id
```

---

## 8. AI 集成方案

### 8.1 DeepSeek API 调用

| 项目 | 值 |
|------|-----|
| API 地址 | `https://api.deepseek.com/chat/completions` |
| 模型 | `deepseek-chat` |
| 认证方式 | `Authorization: Bearer ${apiKey}` |
| 请求格式 | OpenAI 兼容格式 |
| 流式支持 | `stream: true` + SSE 解析 |
| 超时时间 | 60 秒 |
| HTTP 模块 | Node.js 原生 `https`（非 axios） |

### 8.2 AI 功能清单

| 功能 | System Prompt | 温度 | max_tokens | 流式 |
|------|---------------|------|------------|------|
| 笔记整理 | 专业学习笔记整理助手，将转写文本整理为结构化 Markdown | 0.7 | 4096 | ✅ |
| AI 对话 | 学习助手AI，基于笔记内容回答问题 | 0.7 | 4096 | ✅ |
| 笔记摘要 | 生成100字以内摘要，概括核心知识点 | 0.3 | 200 | ❌ |
| 知识分析 | 分析知识点之间的关系和重要程度 | 0.7 | 2048 | ❌ |

### 8.3 对话上下文构建

```typescript
// store.ts — buildNoteContext()
function buildNoteContext(maxChars = 4000): string {
  if (notes.value.length === 0) return ''
  let context = ''
  for (const note of notes.value) {
    const entry = `【${note.title}】\n${note.content}\n\n`
    if (context.length + entry.length > maxChars) break
    context += entry
  }
  return context.trim()
}
```

**策略：** 从最新笔记开始遍历，累计不超过 4000 字符，确保 AI 有足够上下文且不超 token 限制。

### 8.4 多轮对话支持

```javascript
// main.cjs — ai:chat handler
const messages = [
  { role: 'system', content: systemPrompt },
]

// 添加历史对话（最多前6轮 = 12条消息）
if (history && Array.isArray(history)) {
  for (const msg of history.slice(-6)) {
    messages.push({ role: msg.role, content: msg.content })
  }
}

messages.push({ role: 'user', content: question })
```

---

## 9. 数据存储与持久化

### 9.1 存储方案

| 数据类型 | 存储位置 | 格式 | 初始化 |
|----------|----------|------|--------|
| 笔记 | `app/data/notes.json` | JSON 数组 | `[]` |
| 课程 | `app/data/courses.json` | JSON 数组 | `[]` |
| 对话 | `app/data/chatSessions.json` | JSON 数组 | `[]` |
| 统计 | `app/data/stats.json` | JSON 对象 | 空统计对象 |
| 设置 | `app/data/settings.json` | JSON 对象 | `{apiKey: '', model: 'deepseek-chat'}` |
| Electron 缓存 | `app/userdata/` | 系统目录 | 自动创建 |

### 9.2 文件读写函数

```javascript
function readJSON(file, def) {
  try {
    if (fs.existsSync(file)) return JSON.parse(fs.readFileSync(file, 'utf-8'))
  } catch (e) { console.error('读取失败:', file, e) }
  return def
}

function writeJSON(file, data) {
  ensureDataDir()
  fs.writeFileSync(file, JSON.stringify(data, null, 2), 'utf-8')
}
```

### 9.3 数据导出格式

```json
{
  "version": "1.0",
  "exportDate": "2026-08-11T10:00:00.000Z",
  "notes": [...],
  "courses": [...],
  "chatSessions": [...],
  "stats": {...},
  "settings": {...}
}
```

### 9.4 双模式运行

程序支持两种运行模式，由 `store.ts` 中的 `hasElectron` 自动检测：

| 模式 | 检测条件 | 数据存储 | AI 调用 |
|------|----------|----------|---------|
| Electron 模式 | `window.noteAPI` 存在 | 本地 JSON 文件 | IPC → main.cjs → https |
| 浏览器模式 | `window.noteAPI` 不存在 | localStorage | fetch → DeepSeek API |

---

## 10. 安全设计

### 10.1 Electron 安全配置

```javascript
// main.cjs — BrowserWindow 配置
webPreferences: {
  preload: path.join(__dirname, 'preload.js'),
  contextIsolation: true,    // 上下文隔离 ✅
  nodeIntegration: false,    // 禁用 Node 集成 ✅
}
```

### 10.2 API Key 安全

| 环节 | 措施 |
|------|------|
| 存储 | `settings.json` 本地文件，不上传任何服务器 |
| 传递 | API Key 仅存在于主进程，渲染进程通过 IPC 间接调用 |
| HTTP | 使用 `https` 加密传输 |
| 隔离 | `contextIsolation: true` 防止渲染进程直接访问 Node API |
| 显示 | 前端 API Key 输入框可切换显示/隐藏 |

### 10.3 IPC 安全桥接

```javascript
// preload.js — 使用 contextBridge 而非直接暴露 ipcRenderer
contextBridge.exposeInMainWorld('noteAPI', {
  getNotes: () => ipcRenderer.invoke('notes:getAll'),
  // ... 只暴露 invoke 调用，不暴露 on/send
})
```

---

## 11. 构建与运行

### 11.1 环境要求

| 工具 | 最低版本 | 用途 |
|------|----------|------|
| Node.js | 18+ | 运行环境 |
| npm | 9+ | 包管理 |
| Windows | 10 | 目标平台 |

### 11.2 脚本命令

```bash
# 安装依赖
npm install

# 开发模式（Vite 热更新 + Electron）
npm run electron:dev

# 仅启动前端（浏览器调试）
npm run dev

# 构建前端
npm run build

# 启动桌面应用（生产模式）
npm run start

# 打包为 EXE 安装包
npm run electron:build
```

### 11.3 环境变量处理

```json
{
  "scripts": {
    "start": "cross-env NODE_OPTIONS= ELECTRON_RUN_AS_NODE= electron .",
    "electron:dev": "concurrently \"vite\" \"wait-on tcp:5173 && cross-env NODE_OPTIONS= ELECTRON_RUN_AS_NODE= DEV=1 electron .\""
  }
}
```

**为什么需要 `cross-env NODE_OPTIONS= ELECTRON_RUN_AS_NODE=`：**

- `NODE_OPTIONS=` — 清除全局 `--use-system-ca`（Electron 不支持此选项）
- `ELECTRON_RUN_AS_NODE=` — 清除此变量（否则 Electron 以纯 Node.js 模式运行，`require('electron')` 返回路径字符串而非 API 对象）

### 11.4 窗口配置

```javascript
new BrowserWindow({
  width: 1440,          // 默认宽度
  height: 900,          // 默认高度
  minWidth: 1200,       // 最小宽度
  minHeight: 750,       // 最小高度
  title: '笔记星图',
  backgroundColor: '#F5F3FA',
  autoHideMenuBar: true, // 自动隐藏菜单栏
})
```

---

## 12. 打包发布

### 12.1 electron-builder 配置

```json
{
  "build": {
    "appId": "com.notestar.noteanalysis",
    "productName": "NoteAnalysis",
    "directories": { "output": "release" },
    "files": [
      "dist/**/*",
      "electron/**/*"
    ],
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,                    // 非一键安装，有安装向导
      "allowToChangeInstallationDirectory": true,  // 允许选择安装目录
      "createDesktopShortcut": true,        // 创建桌面快捷方式
      "createStartMenuShortcut": true,      // 创建开始菜单快捷方式
      "shortcutName": "NoteAnalysis"
    }
  }
}
```

### 12.2 打包流程

```bash
# 1. 构建前端
vite build  →  dist/

# 2. 打包 Electron
electron-builder --win --x64  →  release/
  ├── NoteAnalysis Setup 1.0.0.exe   # NSIS 安装包
  └── win-unpacked/                   # 免安装版
      └── NoteAnalysis.exe
```

### 12.3 打包后目录结构

```
release/
├── NoteAnalysis Setup 1.0.0.exe    # 安装包（约 80-100MB）
└── win-unpacked/
    ├── NoteAnalysis.exe             # 可执行文件
    ├── resources/
    │   ├── app.asar                 # 打包的应用代码
    │   └── ...
    └── ...（Electron 运行时文件）
```

---

## 13. 已知限制与未来计划

### 13.1 当前限制

| 限制 | 说明 |
|------|------|
| 仅支持 Windows | 当前打包目标为 `win-x64`，未配置 macOS/Linux |
| 单机模式 | 无云同步，数据仅存本地 |
| 单用户 | 无登录系统，无多用户支持 |
| AI 依赖外部 | 需要 DeepSeek API Key，无离线 AI 能力 |
| 无富文本编辑 | 编辑器为纯 textarea，无所见即所得 |
| 无图片支持 | 笔记仅支持文本，不支持图片插入 |
| 无音频直接处理 | 需要用户自行用第三方工具将音频转文字 |

### 13.2 未来计划

| 优先级 | 功能 | 说明 |
|--------|------|------|
| 高 | 富文本编辑器 | 替换 textarea 为 TipTap/ProseMirror |
| 高 | 图片/公式支持 | 支持插入图片和 LaTeX 公式 |
| 中 | macOS 支持 | 添加 darwin 打包目标 |
| 中 | 云同步 | 可选的云端数据同步 |
| 中 | 直接音频转写 | 集成 Whisper API，直接处理音频 |
| 低 | 多用户支持 | 登录系统 + 数据隔离 |
| 低 | 插件系统 | 支持自定义 AI 提示词模板 |
| 低 | 导出 PDF | 笔记导出为 PDF 文件 |

---

## 附录 A：快速开始

### 第一次使用

1. 启动程序
2. 进入「设置」页面
3. 填入 DeepSeek API Key（[获取地址](https://platform.deepseek.com/)）
4. 点击「保存」
5. 进入「笔记整理」页面
6. 点击「导入」→ 选择转写文本文件 或粘贴文本
7. 点击「AI 整理」→ 等待流式生成
8. 笔记自动创建完成

### 日常使用流程

```
录音 → 第三方工具转文字 → 导入 .txt → AI 整理 → 笔记管理 → AI 问答复习
```

---

## 附录 B：IPC 接口快速参考

```typescript
// 完整的 window.noteAPI 接口
window.noteAPI = {
  // 笔记
  getNotes: () => Promise<Note[]>
  getNote: (id: string) => Promise<Note | null>
  saveNote: (note: Partial<Note>) => Promise<Note>
  deleteNote: (id: string) => Promise<boolean>

  // 课程
  getCourses: () => Promise<Course[]>
  saveCourse: (course: Partial<Course>) => Promise<Course>
  deleteCourse: (id: string) => Promise<boolean>

  // 对话
  getChatSessions: () => Promise<ChatSession[]>
  saveChatSession: (session: Partial<ChatSession>) => Promise<ChatSession>
  deleteChatSession: (id: string) => Promise<boolean>

  // 统计
  getStats: () => Promise<Stats>
  addStudyTime: (minutes: number) => Promise<Stats>

  // 文件导入
  importFiles: () => Promise<ImportedFile[] | null>

  // AI
  generateNote: (rawText: string) => Promise<string>
  chatWithAI: (question: string, noteContext: string, history?: ChatMessage[]) => Promise<string>
  summarizeNote: (noteContent: string) => Promise<string>

  // AI 流式监听
  onGenerateNoteChunk: (callback: (chunk: string) => void) => void
  onChatChunk: (callback: (chunk: string) => void) => void

  // 数据管理
  exportData: () => Promise<string | null>
  importData: () => Promise<{ success: boolean; notes: number; courses: number } | null>
  clearData: (type: 'all' | 'notes' | 'chat' | 'stats' | 'courses') => Promise<boolean>

  // 设置
  getSettings: () => Promise<Settings>
  saveSettings: (settings: Settings) => Promise<boolean>
}
```

---

## 附录 C：依赖清单

### 运行时依赖

| 包名 | 版本 | 用途 |
|------|------|------|
| `vue` | ^3.4.21 | 前端框架 |
| `vue-router` | ^4.3.0 | 路由管理 |
| `marked` | ^18.0.9 | Markdown 渲染 |

### 开发依赖

| 包名 | 版本 | 用途 |
|------|------|------|
| `electron` | ^30.0.0 | 桌面框架 |
| `electron-builder` | ^24.13.3 | 打包工具 |
| `vite` | ^5.2.0 | 构建工具 |
| `@vitejs/plugin-vue` | ^5.0.4 | Vue 插件 |
| `typescript` | ^5.4.0 | 类型系统 |
| `vue-tsc` | ^2.0.6 | Vue 类型检查 |
| `concurrently` | ^8.2.2 | 并行执行 |
| `cross-env` | ^7.0.3 | 跨平台环境变量 |
| `wait-on` | ^7.2.0 | 等待端口就绪 |

---

*文档结束 — 笔记星图 NoteStar v1.0.0*
