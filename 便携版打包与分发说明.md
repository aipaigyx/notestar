# 笔记星图 · 便携版打包与分发说明

> 版本：1.0.0（便携绿色版）| 更新日期：2026-08-13

---

## 1. 这是什么

**便携版（绿色版）**：整个程序 + 全部环境依赖（Electron、Python、Whisper 转写模型、Ollama 本地大模型）都放在同一个文件夹里。用户拿到后**双击启动器 exe 即可运行**，无需安装任何东西、无需配置环境变量。

## 2. 目录结构

```
笔记星图/                    ← 整个文件夹压缩后即可分发
├── 笔记星图.exe            ← 【启动器】双击运行（唯一入口）
├── 使用说明.txt            ← （可选的）给用户的使用说明
│
├── app/                    ← 应用本体
│   ├── electron/           ← 主进程代码（main.cjs / preload.js / transcribe.py）
│   ├── dist/               ← 前端构建产物（vite build 生成）
│   ├── node_modules/       ← Electron 运行时与依赖
│   ├── vendor/             ← 内置本地 AI 环境
│   │   ├── python/         ← 便携 Python 3.11（含 faster-whisper 全部依赖）
│   │   └── whisper-models/ ← faster-whisper-medium 转写模型（离线转写用）
│   ├── data/               ← 用户数据（运行时自动创建/读写）
│   └── package.json
│
└── Ollama/                 ← 本地大模型服务（含已下载的模型）
    ├── ollama.exe
    └── models/             ← 模型数据
```

## 3. 使用说明（给最终用户）

1. 将整个文件夹解压到任意位置（**路径不要带中文和空格更稳妥**，如 `D:\笔记星图` 或 `D:\NoteStar`）
2. 双击 **`笔记星图.exe`**，应用即启动（首次启动稍慢属正常）
3. 程序会自动完成两件事：
   - 拉起内置 **Ollama 本地大模型**（用于离线 AI 问答/整理，无需联网）
   - 就绪内置 **Python + Whisper 转写环境**（用于离线音频转文字）
4. 如需使用 DeepSeek 云端模型，在「设置」页填入 API Key 即可

## 4. 分发打包步骤

```bash
# 1. 确认前端已构建
cd app && npm run build        # 生成 app/dist/

# 2. 检查内置依赖完整（均应存在）
app/vendor/python/python.exe
app/vendor/whisper-models/hub/models--Systran--faster-whisper-medium/
Ollama/ollama.exe

# 3. 删除不需要的文件（可选，减小体积）
#    - app/ 下：src/、logs/、release/、userdata*/、test*、verify-dist*、*.log
#    - 根目录：app2/（旧副本）、build/、*.bat、项目说明文档.md
#    - app/node_modules/ 里的 .vite 缓存等

# 4. 全选文件夹 → 右键 → 压缩为 zip/7z
#    建议用 7-Zip 的 .7z 格式（模型文件已压缩，zip 压缩收益低）
```

> 分发前务必用一台**干净机器**（或删除 `app/data/` 与 `app/userdata/`）测试，确保没有携带个人数据。

## 5. 内置依赖说明

| 组件 | 位置 | 大小参考 | 用途 |
|------|------|----------|------|
| Electron 30 | app/node_modules/electron/ | ~250MB | 桌面应用运行时 |
| Python 3.11 便携 | app/vendor/python/ | ~300MB | 运行转写脚本 |
| faster-whisper + 依赖 | app/vendor/python/Lib/site-packages/ | ~286MB | 语音转写引擎 |
| Whisper medium 模型 | app/vendor/whisper-models/ | ~1.5GB | 离线转写模型 |
| Ollama + 模型 | Ollama/ | ~7.7GB | 本地大模型服务 |

**启动器工作原理**：`笔记星图.exe`（约 7MB，PyInstaller 单文件）双击后：
1. 定位自身所在目录（= 分发文件夹根）
2. 用 `app/node_modules/electron/dist/electron.exe` 加载 `app/` 目录
3. 应用主进程自动查找同目录的 `Ollama/` 和 `app/vendor/`，拉起本地服务

## 6. 重新构建启动器（可选）

```bash
# 需要 Python + PyInstaller 的环境
cd build
python -m PyInstaller --onefile --noconsole --name NoteStarLauncher \
  --distpath ".." --workpath ./pyinstaller-work --specpath . launcher.py
# 生成后重命名为 笔记星图.exe
```

## 7. 注意事项

- **杀毒软件**：PyInstaller 打包的 exe 可能被部分杀软误报，可加白名单
- **首次启动**：Ollama 冷启动 + 模型加载约需 10~30 秒，属正常
- **端口**：Ollama 默认使用 11434 端口，若被占用会走兜底路径（应用内提示）
- **数据安全**：所有数据存于 `app/data/`，分发时请先清空或自行处理
- **升级**：替换 `app/electron/`、`app/dist/` 即可升级应用，`data/`、`Ollama/models/` 保留

---

*便携版说明 — 笔记星图 NoteStar v1.0.0*
